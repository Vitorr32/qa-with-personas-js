# Production environment variables (example)
# Copy this to .env.prod and fill in values. Do NOT commit .env.prod.

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
OPENAI_ANALYSIS_MODEL=gpt-4o-mini
OPENAI_FILE_TTL_SECONDS=3600

# Database (compose provides these, override if needed)
DB_USERNAME=myuser
DB_PASSWORD=123
DB_HOST=db
DB_NAME=qa_with_personas
DB_PORT=5432

# Backend server
PORT=5000
FRONTEND_URL=http://localhost
FRONTEND_PORT=80

# One-time schema sync on first boot (set to true for initial deploy, then remove/disable)
DB_SYNC_ON_BOOT=false
